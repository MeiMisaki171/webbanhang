services:
  postgres:
    image: postgres:16
    container_name: dgdp-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dien_gia_dung_pro
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
